# Todos — 2026-09-06

Created: 2026-09-06. Morning follow-ups after the management deploy. Not started.

1. ~~Console breadcrumb stays `Client -> Project -> None` after reload until another click.~~ Done 2026-09-06.
2. ~~Repeated Create stacks identical same-name tech links; revoke one revokes all.~~ Done 2026-09-06.
3. ~~Event test rows: no identity in Button Identity; Viewport shows `No` instead of blank.~~ Done 2026-09-06.
4. ~~Test dialogue Fail is the wrong color when ready for retest (every device, not iPhone-only).~~ Done 2026-09-06.
5. ~~Viewport popup: no usable page-count indicator while navigating frames (shell drops `.dot` / `.vp-indicator` styles).~~ Done 2026-09-06.
6. ~~Disabled test type in Settings: test dialogue must say `<<test target>> are not included in testing` (e.g. `Text Labels are not included in testing`).~~ Done 2026-09-06.
7. ~~Mouse marquee on the device page must not select buttons inside a viewport. Viewport contents are selectable only in the viewport zoom viewer.~~ Done 2026-09-06.

## Housekeeping — unmanaged storage loops

Live snapshot 2026-09-06: 287 tech links (55 live / 232 revoked), 12 `.apex` files, 47 empty upload dirs, 34 generated dirs with no project (1.3 GB generated total), 30 live `Resilience *` test projects.

8. ~~Stop minting a second live link for the same technician name (same as item 2). Reuse the active link; do not create another row.~~ Done 2026-09-06.
9. Purge or hide revoked tech links and tokens so they are not the working list. Revoke must not leave 40 dead rows (Sung is 40/1/39).
10. Delete empty/orphan `uploads/` dirs. Keep one `.apex` file and one DB upload row per project (today: prune keeps two rows, disk one file; `POST /uploads` and failed generate do not prune).
11. Delete `generated/` dirs with no matching project. Add a retire path so abandoned jobs do not leave HTML trees.
12. Wipe to a fresh database: delete all clients, projects, and related job data (not only `Resilience *`). Keep tests off production Postgres. Empty/orphan disk cleanup (10–11) applies to the leftover trees after that wipe.
